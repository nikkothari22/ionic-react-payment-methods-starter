import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'op-payment-demo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
